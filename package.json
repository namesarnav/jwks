{
    "name": "jwks-server",
    "version": "1.0.0",
    "description": "JWKS server with SQLite storage",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "mocha --timeout 5000",
        "coverage": "nyc --reporter=html --reporter=text mocha --timeout 5000"
      }, 
    "author": "Arnav Verma",
    "license": "ISC",
    "dependencies": {
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.2",
        "pem-jwk": "^2.0.0",
        "sqlite3": "^5.1.7"
    },
    "devDependencies": {
        "mocha": "^10.8.2",
        "nyc": "^15.1.0",
        "supertest": "^6.3.4"
    }
}
